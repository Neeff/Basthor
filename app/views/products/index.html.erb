<p id="notice"><%= notice %></p>
<h1>Products</h1>
<%= form_with(url: search_shop_products_path) do |f| %>
  <%= f.label :Buscar_Producto %>
  <%= f.text_field :search %>
  <% end %>

  <%= link_to  "Shop",shop_path(@shop), class: 'btn btn-success' %>
  <div class="row">
      <div id="products">
        
      </div>

    </div>
<script>
  $('#search').keyup(function(){
    $.ajax({
      type: "GET",
      url: "<%= search_shop_products_path%>",
      data: {search: $('#search').val()},
      success: function (response) {
              
      }
});
  });
</script>