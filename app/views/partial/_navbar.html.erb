<nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#"><%= image_tag ("Marca"), size: '75x75' %></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="<%= root_path%>">Home </a>
            </li>
            <% if user_signed_in? %>
            <li class="nav-item pull-xs-right">
                <a class="nav-link" href="<%= destroy_user_session_path%>" data-method="DELETE">Cerrar Sesion</a>
              </li>
            <% else %>
            <li class="nav-item pull-xs-right">
                <a class="nav-link" href="<%= new_user_session_path%>">Iniciar Sesion</a>
              </li>
              <li class="nav-item pull-xs-right">
                  <a class="nav-link " href="<%= new_user_registration_path%>">Crear Cuenta</a>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>